---
import ArticleSearchBarInner from './ArticleSearchBarInner';

import {
  getAllArticles,
} from '@/utils/pathUtil';

const allArticles = getAllArticles();
---

<ArticleSearchBarInner client:visible articles={allArticles} />
