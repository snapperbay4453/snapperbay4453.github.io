---
import DirectoryCardInner from './DirectoryCardInner';

const { title, imagePath, backgroundColorPalette, onClick } = Astro.props;
---

<DirectoryCardInner
  title={title}
  imagePath={imagePath}
  backgroundColorPalette={backgroundColorPalette}
  onClick={onClick}
  client:only='preact'
/>
