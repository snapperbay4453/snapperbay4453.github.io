---
const { title, subtitle } = Astro.props;
---

<div class='max-w-5xl mx-auto px-4 5xl:px-0'>
  <div class='flex flex-col sm:flex-row max-w-3xl mx-auto'>
    <div class='grow flex flex-col justify-center items-center sm:items-start gap-2 flex-wrap h-40 sm:w-1/2'>
      <p class='text-3xl font-bold ml-0 sm:ml-16 text-center sm:text-left'>{title ?? ''}</p>
      <p class='text-2xl font-bold ml-0 sm:ml-16 text-center sm:text-left'>{subtitle ?? ''}</p>
    </div>
    <div class='grow flex justify-center items-start sm:items-center gap-2 h-20 sm:h-40 sm:w-1/2'>
      <slot />
    </div>
  </div>
</div>
